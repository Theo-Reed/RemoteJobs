/* Global Container */
.login-wall {
  --anchor-y: 45.45vh; /* 5/11 处作为全局对齐锚点 */
  position: fixed;
  top: 0;
  left: 0;
  width: 100vw;
  height: 100vh;
  z-index: 10000;
  visibility: hidden;
  pointer-events: none;
  background: transparent !important;
}

.login-wall.visible {
  visibility: visible;
  pointer-events: auto;
}

/* Background Layer - 动态背景切换 🌓 */
.glass-mask {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  transition: all 1.2s cubic-bezier(0.23, 1, 0.32, 1);
  z-index: 0;
  opacity: 1;
}

/* 初始 Splash 状态：纯白背景，遮挡主页面 */
.glass-mask.splash {
  background: #ffffff !important;
  backdrop-filter: blur(0px);
  -webkit-backdrop-filter: blur(0px);
}

/* 转换到 Login 状态：背景变为透明毛玻璃，透出主页面 */
.glass-mask.login {
  background: rgba(255, 255, 255, 0.03) !important; 
  backdrop-filter: blur(5px) brightness(102%); 
  -webkit-backdrop-filter: blur(5px) brightness(102%);
}

/* 隐藏/成功状态 */
.glass-mask.hidden {
  opacity: 0;
  backdrop-filter: blur(0px);
  -webkit-backdrop-filter: blur(0px);
}

/* --- Shared Animated Globe (The Engine 🎨) --- */
.shared-globe {
  position: fixed;
  z-index: 1001;
  /* 增加 GPU 预渲染提示 */
  will-change: transform, opacity, width, height;
  /* 更丝滑的弹性曲线 */
  transition: all 0.8s cubic-bezier(0.23, 1, 0.32, 1);
  pointer-events: none;
  overflow: visible !important;
  display: flex;
  align-items: center;
  justify-content: center;
  /* 初始强制硬件加速 */
  transform: translate3d(0, 0, 0);
  backface-visibility: hidden;
  perspective: 1000px;
}

.ai-star {
  width: 100%;
  height: 100%;
  /* 呼吸动画也改为 3D 变换以保持在同一渲染层 */
  animation: ai-breathe 4s infinite ease-in-out;
  will-change: transform;
  /* 增加主题蓝色外发光，增强质感 */
  filter: drop-shadow(0 0 15rpx rgba(46, 95, 233, 0.4));
}

@keyframes ai-breathe {
  0%, 100% { transform: translate3d(0,0,0) scale(0.85) rotate(-2deg); opacity: 0.9; }
  50% { transform: translate3d(0,0,0) scale(1.05) rotate(2deg); opacity: 1; }
}

/* State 1: Splash (Centered, Large) */
.shared-globe.splash {
  width: 500rpx;
  height: 500rpx;
  top: 0; 
  left: 0;
  /* 使用 translate3d 进行绝对定位，避开 Layout 抖动 */
  transform: translate3d(calc(50vw - 250rpx), calc(50vh - 250rpx), 0);
}

/* State 2: Login (与卡片锚点锁定同步) */
.shared-globe.login {
  width: 160rpx;
  height: 160rpx;
  top: 0;
  left: 0;
  /* 
     锚点同步计算：
     X: 保持左位微调落点
     Y: 基于全局锚点变量向上偏移，确保所有机型相对比例一致
  */
  transform: translate3d(calc(11vw + 18rpx), calc(var(--anchor-y) - 200rpx), 0);
}

/* 授权加载状态：移位到卡片中心 */
.shared-globe.auth-loading {
  transform: translate3d(calc(50vw - 80rpx), calc(var(--anchor-y) - 80rpx), 0) !important;
}

/* 授权过程中的呼吸动画（更律动，体现正在处理） */
.auth-loading .ai-star {
  animation: auth-breathing 1.5s infinite ease-in-out !important;
}

@keyframes auth-breathing {
  0%, 100% { transform: scale(0.9); opacity: 0.85; }
  50% { transform: scale(1.1); opacity: 1; }
}

/* State 3: Hidden (Success Fade Out) */
.shared-globe.hidden {
  opacity: 0;
  transform: scale(0.8) translate3d(calc(50vw - 80rpx), calc(var(--anchor-y) - 80rpx), 0);
  pointer-events: none;
}


/* --- Phase 1: Splash Content --- */
.splash-content {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  transition: opacity 0.5s ease;
  z-index: 2;
  pointer-events: none;
}

.splash-content.fade-out {
  opacity: 0;
}


/* --- Phase 2: Login Card --- */
.login-card {
  position: fixed; 
  left: 50%;
  top: var(--anchor-y); /* 使用全局锚点变量 */
  /* 使用 -50% 让卡片自身的中轴线对齐锚点 */
  transform: translate(-50%, -40%); 
  
  width: 78vw; 
  background: rgba(255, 255, 255, 0.82); 
  backdrop-filter: blur(20px);
  -webkit-backdrop-filter: blur(20px);
  
  border-radius: 64rpx;
  padding: 64rpx 48rpx 40rpx 48rpx; /* 减少底部内边距 */
  box-shadow: 0 40rpx 100rpx rgba(0, 0, 0, 0.15);
  
  opacity: 0;
  transition: all 0.6s cubic-bezier(0.23, 1, 0.32, 1);
  pointer-events: none;
  z-index: 10;
  display: flex;
  flex-direction: column;
}

.login-card.show {
  opacity: 1;
  transform: translate(-50%, -50%); /* 最终落位在 5/11 处 */
  pointer-events: auto;
}

/* 授权成功，整体渐渐淡出 */
.login-card.hide-all {
  opacity: 0 !important;
  pointer-events: none;
  /* 不移动，保持在原地 */
  transform: translate(-50%, -50%) !important;
}

/* 内部内容淡出逻辑 */
.card-inner-content {
  opacity: 1;
  transition: opacity 0.5s ease;
  width: 100%;
  display: flex;
  flex-direction: column;
}

.card-inner-content.content-fade-out {
  opacity: 0;
  pointer-events: none;
}

/* Provide space for the shared lottie engine */
.logo-placeholder {
  width: 140rpx; /* Slightly smaller than canvas to feel snug */
  height: 140rpx;
  margin-right: 30rpx;
  flex-shrink: 0;
  /* Invisible space */
}

/* Status Banner */
.status-banner {
  height: 0;
  overflow: hidden;
  background: #fef2f2;
  border-radius: 24rpx;
  margin-bottom: 0;
  transition: all 0.5s cubic-bezier(0.19, 1, 0.22, 1);
  opacity: 0;
}

.status-banner.active {
  height: 80rpx;
  margin-bottom: 30rpx;
  opacity: 1;
}

.banner-content {
  height: 80rpx;
  display: flex;
  align-items: center;
  padding: 0 30rpx;
}

.banner-text {
  font-size: 24rpx;
  color: #991b1b;
  font-weight: 500;
}

/* Card Header */
.card-header {
  display: flex !important;
  align-items: center !important;
  margin-bottom: 30rpx !important; /* 从 50rpx 减少到 30rpx，压缩标题下方间距 */
  flex-direction: row !important;
}

.title-group {
  text-align: left !important;
}

.main-title {
  display: block;
  font-size: 44rpx;
  font-weight: 800;
  color: #1e293b;
  margin-bottom: 4rpx;
}

.sub-title {
  display: block;
  font-size: 24rpx;
  color: #64748b;
  font-weight: 500;
}

/* Form */
.auth-section {
  display: flex;
  flex-direction: column;
  gap: 32rpx;
  padding: 20rpx 0;
}

.auth-tips {
  text-align: center;
}

.tips-text {
  font-size: 26rpx;
  color: #64748b;
  line-height: 1.6;
}

.phone-btn {
  gap: 16rpx;
}

.btn-icon {
  width: 44rpx;
  height: 44rpx;
}

.auth-desc {
  font-size: 24rpx;
  color: #94a3b8;
  text-align: center;
}

.primary-btn {
  width: 100% !important;
  height: 110rpx;
  background: linear-gradient(135deg, #3b82f6, #2563eb) !important;
  color: #ffffff !important;
  border-radius: 32rpx !important;
  font-size: 32rpx !important;
  font-weight: 700 !important;
  display: flex !important;
  align-items: center !important;
  justify-content: center !important;
  margin-top: 20rpx !important;
  box-shadow: 0 20rpx 40rpx rgba(37, 99, 235, 0.2) !important;
}

.card-footer {
  margin-top: 40rpx;
  text-align: center;
}

.protocol {
  font-size: 22rpx;
  color: #94a3b8;
}

/* Animations */
@keyframes breathe {
  0%, 100% { transform: scale(1); }
  50% { transform: scale(1.05); }
}

@keyframes wave {
  0%, 80%, 100% { transform: scale(0.8); opacity: 0.3; }
  40% { transform: scale(1.5); opacity: 1; }
}

.error-layer {
  display: none !important;
}
